# Project Repository

This monorepo contains the Ping Pong League frontend.

- Frontend: ping_pong_frontend (React)

Environment variables are managed via .env files in each container. For the frontend, set REACT_APP_API_BASE_URL in ping_pong_frontend/.env